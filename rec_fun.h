#include <iostream>
using namespace std;

//im not gonna use class member functions because the functions are already given
//meaning that theres no need to create private and or public classes
//because we all have access to the same recursive functions

void binary_print(ostream& outs, unsigned int n);
void triangle(ostream& outs, unsigned int num, unsigned int limit);
double pow(double x, int n);
void indented_sentences(size_t m,size_t n);
